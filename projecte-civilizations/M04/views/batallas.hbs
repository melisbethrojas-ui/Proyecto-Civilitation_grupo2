<div class="contador-destacat">
    <h3>Total de batallas jugadas: <span class="numero-total">{{totalBatallas}}</span></h3>
</div>

<h1>Historial de reportes de batalla</h1>

{{#if batallas}}
    {{#each batallas}}
    <div class="battle-card" style="border: 2px solid #ccc; margin: 15px 0; padding: 15px; background: #222; color: #fff; border-radius: 8px;">
        <div class="battle-header" style="background: #444; padding: 10px; font-weight: bold;">
            <h3>Batalla #{{this.id}} — ¡{{this.resultado}}!</h3>
        </div>
        <div class="battle-body" style="padding: 10px;">
            <p><strong>Resultado de la simulación:</strong> {{this.resultado}}</p>
            <p><a href="/informe?informe={{this.id}}" style="color: #00ffcc; font-weight: bold;">Ir al informe detallado de la batalla →</a></p>
        </div>
    </div>
    {{/each}}
{{else}}
    <p>Aún no se han registrado batallas en el sistema.</p>
{{/if}}

<p style="margin-top: 20px;"><a href="/">Volver a la portada</a></p>